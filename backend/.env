PORT=3000
JWT_SECRET=supersecret_jwt_key_change_me

# Local development database (via docker-compose db service exposed on 5433)
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/blackbank

# Docker Compose internal database URL (used inside api container)
# DATABASE_URL=postgresql://postgres:postgres@db:5432/blackbank

